# Environment variables — never commit credentials
.env
.env.local
.env.*.local

# Vercel CLI output
.vercel

# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# Node (if dependencies added later)
node_modules/
npm-debug.log*
yarn-error.log*
